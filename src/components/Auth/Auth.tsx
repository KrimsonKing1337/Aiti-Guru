import LogoIcon from 'assets/icons/logo.svg';

import { Wrapper, Header } from './components';

export const Auth = () => {
  return (
    <Wrapper>
      <LogoIcon />
      <Header />
    </Wrapper>
  );
};
