import LogoIcon from 'assets/icons/logo.svg';

import { Wrapper, Header, Inputs } from './components';

export const Auth = () => {
  return (
    <Wrapper>
      <LogoIcon />
      <Header />
      <Inputs />
    </Wrapper>
  );
};
